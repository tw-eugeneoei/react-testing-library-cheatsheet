import { TextField } from "@mui/material";

const TextInput = () => {
    return (
        <TextField
            label="Text Input Element"
            variant="outlined"
            placeholder="Required..."
        />
    );
};

export { TextInput };
