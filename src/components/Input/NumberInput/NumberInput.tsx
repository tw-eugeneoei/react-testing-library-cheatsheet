import { TextField } from "@mui/material";

const NumberInput = () => {
    return (
        <TextField
            label="Number Input Element"
            type="number"
            variant="outlined"
        />
    );
};

export { NumberInput };
